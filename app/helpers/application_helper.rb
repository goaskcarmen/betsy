module ApplicationHelper
	# shows that we can write a view helper, but we won't use because there is a built in rails helper that does this
	# def to_money(cents)
	#   ("<span class='dollar_amount'>" + Money.new(cents,"USD").format + "</span>").html_safe
	# end
end
