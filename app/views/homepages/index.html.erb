<% if @user %>
<h1>Welcome <%= @user.name %></h1>
<% else %>
<h1>please log in</h1>
<% end %>


<% if flash[:notice] %>
<p class = "flash-notice"><%= flash[:notice] %></p>

<% end %>
<!--<%= to_money(400) %> that is an example of how to use to_money-->

<!-- ########### Products ################### -->

<div class="index-photo">
  <% @products.each do |p| %>

    <%= link_to image_tag(p.photo_url, :title => p.name, :alt => p.name), product_path(p), method: :get %>

  <% end %>
<!--  <%=  image_tag("/assets/images/products/#{@products.first.photo_url}", :title => @products.first.name) %>
  <%= Product.all.first.photo_url %>-->
</div>
