
<h1><%= @product.name %></h1>
<div class="row">
  <div class="large-4 columns">
    <!-- product picture: -->
    <%= image_tag(@product.photo_url, :title => @product.name, :alt => @product.name) %>
    <div class="hide-for-small panel">

      <h3> sold by: <%= @product.user.name %></h3>

      <h5 class="subheader">blah blah blah stuff about the category</h5>
    </div>
    <a href="#"><div class="panel callout radius"><h6>something somthing...</h6></div></a>
  </div>



<!-- <h1><%= @product.name %></h1> -->
  <div class="large-8 columns">
    <p> Price: <%= number_to_currency(@product.unit_price) %></p>
    <p><% if @product.quantity == 0 %>
       <%= "Out of Stock" %>
       <% else %>
       <%= @product.quantity %> left in stock.
       <% end %>
    </p>
    <p><%= @product.description %></p>
    <ul>
      <li class="round button">
        <%=link_to "Add item to cart", create_cart_product_path(@product), method: :post %>
      </li>
      <li class="round button">
      <%= link_to "View my cart", show_cart_product_path, method: :get %>
      </li>
    </ul>
  </div>
</div>

<!-- Insert reviews here -->

<%= render partial: "reviewsform" %>

<% if flash[:notice] %>
<p class = "flash-notice"><%= flash[:notice] %></p>

<% end %>
<!-- dispaly all product's reviews here using @reviews.each do  -->
<% @reviews.each do |r| %>
  <ul>
    <li>
      pears grade:
      <% (r.rating).times do %>
      <%= "🍐" %>
      <% end %>
    </li>
    <li>
      review contect:
      <%= r.content %>
    </li>
  </ul>
<% end %>


<!-- ONLY FOR MERCHANTS -->
<% if @merchant and @merchant.id == @product.user_id %>
	<%= button_to "Edit Item", edit_product_path(@product), method: :get %>
	<%= button_to "Delete Item", product_path(@product), method: :delete, data: { confirm: "Are you sure?" } %>
<%end%>




<!-- ONLY FOR MERCHANTS -->
