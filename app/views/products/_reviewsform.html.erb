<section>
  <%= form_for @review, url: product_reviews_path(@product.id) do |f| %>
    <%= f.label :rate_product %>
    <%= f.select :rating, (1..5)%>
    <%= f.text_field :content, placeholder: "add your review here" %>

    <p><%= f.submit "Save" %></p>
  <% end %>
</section>
